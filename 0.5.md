sequenceDiagram
    participant browser
    participant server

    browser->>server: GET /spa
    activate server
    server-->>browser: SPA HTML, CSS, JS
    deactivate server

    Note right of browser: Browser executes SPA JavaScript

    browser->>server: GET /notes (fetch existing notes via API)
    activate server
    server-->>browser: JSON with existing notes
    deactivate server

    Note right of browser: Browser renders the notes dynamically without reloading the page
