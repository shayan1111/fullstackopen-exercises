sequenceDiagram
    participant browser
    participant server

    browser->>server: POST /notes with new note data
    activate server
    server-->>browser: Confirmation + updated notes list
    deactivate server

    Note right of browser: Browser updates the page with the new note
