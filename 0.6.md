sequenceDiagram
participant browser
participant server

    browser ->> server: get /spa
    activate server
    server -->> browser: SPA HTML, CSS, JS
    deactivate server

    Note right of browser: The user asks the server for data using GET, and the server responds with the HTML, CSS and JavaScript

    browser->>server: POST /notes with new note data
    activate server
    server-->>browser: Confirmation + updated notes list
    deactivate server

    Note right of browser: Browser updates the page dynamically with the new note
